#include <stdio.h>

void main(){

    int x, y, z;

    printf("Inputkan nilai x: ");
    scanf("%d", &x);

    printf("Inputkan nilai y: ");
    scanf("%d", &y);

    z = x + y;

    printf("Hasil x + y: %d", z);

}
